export const createCorn = () => {
    const cornObject = {
        type: 'Corn',
        height: 180,
        output: 6
    }
    const cornObjectArray = [cornObject, cornObject]
    
    return cornObjectArray
}
